<?xml version="1.0" encoding="utf-8"?>
<!--
  The engine serves on-demand file reads over plain HTTP on loopback, so
  cleartext has to be permitted for it. It is permitted *only* for loopback:
  everything Syncthing does between devices is its own encrypted protocol and
  must not fall back to cleartext.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
